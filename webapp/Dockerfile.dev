FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source (volume mount in docker-compose will override)
COPY . .

# Expose port
EXPOSE 5173

# Run dev server with host binding for container access
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
