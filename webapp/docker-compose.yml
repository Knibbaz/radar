version: '3.8'

services:
  radar-webapp:
    build: .
    container_name: capsule-radar-webapp
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
